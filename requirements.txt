# ==================== 基础网络 ====================
requests>=2.31.0
httpx>=0.28.1

# ==================== 配置与序列化 ====================
python-dotenv==1.0.1
pyyaml>=6.0
pydantic>=2.6.0

# ==================== PDF 处理 ====================
PyMuPDF==1.24.0

# ==================== 文献检索 ====================
metapub>=0.5.0
biopython>=1.84

# ==================== 文献下载 ====================
paperscraper>=0.3.0

# ==================== 数据分析 ====================
numpy>=1.20.0

# ==================== 相关度分析 ====================
rank-bm25>=0.2.2

# ==================== BGE-M3 嵌入模型（RAGFlow 混合检索依赖）====================
# BGEHybridRetriever 在运行时加载 BAAI/bge-m3 权重（首次自动下载 ~1.3 GB）
# Docker 镜像构建时预先运行：
#   python -c "from sentence_transformers import SentenceTransformer; SentenceTransformer('BAAI/bge-m3')"
sentence-transformers>=2.2.0

# ==================== AgentTemplate 层核心依赖 ====================

# LLM 调用层（provider-agnostic）
openai>=1.60.0
litellm>=1.82.0

# LangGraph & LangChain
langgraph>=0.5.4
langchain-core>=0.2.0
langchain-litellm>=0.1.0

# ==================== 数据库 ====================
sqlalchemy>=2.0
psycopg[binary]>=3.1

# ==================== 算子调优工具（scripts/debugger/ Streamlit UI） ====================
streamlit>=1.35.0

# ==================== 对话式 CLI（backend/src/cli/） ====================
# rich：终端 UI 库，提供 Panel/Table/Live 进度面板和 ANSI 颜色支持
# 已是 streamlit 的间接依赖，此处显式声明以确保版本和可用性
rich>=13.0

# ==================== 可观测性（LLM Tracing） ====================
# Langfuse：LLM 全链路追踪（token/cost/latency/prompt-output）
# 配置 LANGFUSE_PUBLIC_KEY + LANGFUSE_SECRET_KEY 后自动启用，未配置时静默跳过
langfuse>=2.0