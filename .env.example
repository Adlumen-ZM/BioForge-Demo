# 复制此文件为 .env 并填入真实值
# cp .env.example .env
DOCKER_IMAGE=bioforge
DOCKER_TAG=local

# LLM API Keys
MINIMAX_API_KEY=your_minimax_api_key_here
MINIMAX_GROUP_ID=your_group_id_here

# Database
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=bioforge
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=bioforge

# RAG
EMBEDDING_MODEL=BAAI/bge-m3

# LangGraph
LANGGRAPH_CHECKPOINT_DB_URL=postgresql://bioforge:your_password_here@localhost:5432/bioforge

# Trace MVP — 写入 agent_trace_events 表（与业务库同实例同库）
# 若使用 psycopg3 驱动（requirements.txt 中已安装），连接串前缀改为 postgresql+psycopg://
# 若使用默认 psycopg2，保持 postgresql:// 即可
TRACE_DB_URL=postgresql://bioforge:your_password_here@localhost:5432/bioforge
